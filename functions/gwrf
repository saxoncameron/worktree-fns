# Force-remove a worktree using the same control flow as "gwr".
#
# Delegating to "gwr" keeps the completion behavior, repo-root fallback, and
# any future deletion diagnostics in one place.
gwrf() {
  gwr --force "$@"
}
